# Copie ce fichier en ".deploy.env" (sans .example) et colle ton secret.
# Le déploiement (npm run deploy:pages) le lira automatiquement pour activer
# la revue de code du diff. Fichier ignoré par git — ton secret reste local.
POSTSTAY_SECRET=colle_ici_ton_secret_POSTSTAY_SECRET
