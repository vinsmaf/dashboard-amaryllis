# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Wrangler local state
.wrangler/

# Claude local skills / scratch files (do not commit)
.claude/

# Zip archives
*.zip

# Variantes images responsive générées au build (web-010)
public/photos/**/*-[0-9]*w.webp

# Revue visuelle — screenshots locaux
bug-reports/

# Secret local pour la revue de code au déploiement (NE PAS committer)
.deploy.env

# Rapport d'audit rolling auto (régénéré à chaque deploy) — les rapports datés restent suivis
docs/_audits/AUDIT-latest.md

# Variables locales wrangler (secrets test/dev — NE JAMAIS committer)
.dev.vars

# Données sensibles — jamais commitées (codes accès, intel concurrentielle)
public/guides/*.json
public/competitors/*.csv

# Sortie générée par le skill graphify (knowledge graph) — ne pas committer
graphify-out/

# Booking.com scraper — session + état (NE JAMAIS committer)
.booking-session.json
.booking-state.json
