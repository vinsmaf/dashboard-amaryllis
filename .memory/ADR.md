# ADR — Décisions structurantes (locatif-dashboard)

> 1 entrée par décision qui engage la suite. Format 5 lignes : **Choix · Alternatives refusées · Conséquences attendues · Périmètre · Statut**.
> Décisions d'archi détaillées (specs complets) → `../docs/superpowers/specs/README.md` (ADR-001→010). Ici = log curaté de session.
> **Archive mensuelle** : les décisions antérieures au mois courant vivent dans `.memory/archive/ADR-YYYY-MM.md`. Archive actuelle : [`archive/ADR-2026-06.md`](./archive/ADR-2026-06.md) (114 entrées, juin 2026).

## ADR-DOCS-ENDPOINTS-001 · 2026-07-05 · Documentation complète des 149 endpoints + garde-fou anti-dette permanent

1. **Choix** : Vincent a demandé la mise à jour de la doc endpoints (ticket Webmaster MOYENNE), scope initialement limité aux changements récents puis explicitement élargi ("check tout les trous je veux que l'on soit clean"). Audit complet `functions/api/` vs `CLAUDE.md` (comparaison par ROUTE servie, pas par nom de fichier — `foo/[[path]].js` → `/api/foo`) : 149 endpoints réels, 66 documentés, 83 manquants. 6 sous-agents Explore parallèles ont lu les 83 fichiers et renvoyé les lignes de tableau prêtes à l'emploi (context budget préservé côté session principale). Insertion dans les sections existantes + 2 nouvelles sections (Newsletter, Éditorial & Réseaux sociaux). Ajout d'un 7ᵉ invariant (`INV7`) à `scripts/audit-invariants.mjs` (déjà greffé non-bloquant dans `deploy-pages.sh`) : compare chaque route réelle à `CLAUDE.md`, 🟡 RISK si absente — pour que cette dette ne revienne jamais silencieusement.
2. **Alternatives refusées** : documenter seulement les endpoints touchés cette session (7) — explicitement dépassé par la demande de Vincent. Créer un nouveau script de vérification séparé — rejeté, `audit-invariants.mjs` existait déjà avec exactement ce pattern (non-bloquant, rapport `docs/_audits/AUDIT-latest.md`), l'étendre était plus simple qu'en dupliquer un.
3. **Conséquences attendues** : le premier run d'`INV7` a immédiatement trouvé 2 endpoints oubliés par le rattrapage manuel (`inventory.js`, `maintenance.js`) — preuve que la détection déterministe complète la lecture humaine, pas l'inverse. Root cause identifiée : 119 commits ajoutant un endpoint sur 60 jours vs 21 touchant `CLAUDE.md` — aucun réflexe systématique "je code un endpoint → je documente", la doc suivait au fil de l'eau sans jamais de passe de rattrapage dédiée pour les familles nées après le 25/05 (agents IA, éditorial/social, CRM, WhatsApp).
4. **Périmètre** : `CLAUDE.md` (+92 puis +3 lignes) · `scripts/audit-invariants.mjs` (+INV7, `readdirSync` récursif sur `functions/api/`).
5. **Statut** : ✅ déployé (commits `4527abd`, `197f4b6`). Vérifié en live : `node scripts/audit-invariants.mjs` → 🟢 PASS, 0 route manquante après ajout des 2 oublis.

## ADR-AGENTS-TRIAGE-002 · 2026-07-05 · Triage backlog — 2 règles fiables ajoutées (entité inventée + doublon contenu publié)

1. **Choix** : en triant manuellement le backlog `agent_actions` avec Vincent (35 items backlog + `traf-049`/`traf-016`), 2 hallucinations ont échappé au triage automatique existant (`ADR-AGENTS-TRIAGE-001`) : `traf-049` visait une entreprise "GreenTech" totalement inexistante (marquée `fait` à tort le 23/06) et `traf-016` proposait un article de blog sur un sujet déjà publié (`reservation-directe-villa-martinique`). Vincent a demandé de renforcer le cron. Ajout de 2 règles au prompt LLM de `agents-triage.js` : RÈGLE 5 (toute entité/villa/client hors des 7 biens + "Amaryllis Locations" = hallucination, bloque TOUJOURS) et RÈGLE 6 (requête live `seo_articles WHERE status='published'` injectée dans le prompt, bloque si chevauchement net avec un article déjà publié). Contrairement à la catégorie 4 (digest statique, signal FAIBLE), les catégories 5 et 6 s'appuient sur des requêtes LIVE (`_biens.js` importé + D1 direct) donc classées FIABLES.
2. **Alternatives refusées** : détection déterministe par regex des noms d'entité (extraction de noms propres capitalisés hors allowlist) — rejetée, trop de faux positifs prévisibles (Sainte-Luce, Stripe, WhatsApp sont aussi des noms propres légitimes) ; le LLM avec des règles explicites et un exemple concret (GreenTech) est plus fiable ici qu'une heuristique regex fragile.
3. **Conséquences attendues** : le cron hebdo (lundi) catchera désormais ces 2 classes d'erreur automatiquement. Testé en `?dry=1` en live après déploiement : 59 items analysés, 3 bloqués (tous règle 2 préexistante, contradictions de prix Mabouya), 0 faux positif sur les 2 nouvelles règles sur ce lot. Pas encore observé un vrai positif règle 5/6 en conditions réelles (les 2 cas trouvés avaient déjà été corrigés manuellement avant le déploiement) — à surveiller au prochain lundi.
4. **Périmètre** : `functions/api/agents-triage.js` (+règles 5/6, +requête `seo_articles`, +fonction `articlesText()`).
5. **Statut** : ✅ déployé (commit `56f4c45`) & vérifié en live (`?dry=1`). Au passage, 35 items du backlog triés manuellement avec Vincent ce jour : 5 doublons fermés (déjà dans `AGENDA.md` depuis la session "réunion générale" du 29/06), 1 bloqué par règle absolue (dépense pub `traf-050`, jamais auto-exécutée), 2 signalés sans rejet (imprécision géographique `cm-012`, cluster de 3 quasi-doublons Mabouya `cm-063/071/075`), le reste (~26) laissé en backlog business (pas de problème factuel détecté, priorisation = décision de Vincent).

## ADR-RAG-DOCS-001 · 2026-07-04 · RAG ingère les docs stratégiques + fraîcheur quotidienne scopée (jamais légal/stratégie)

1. **Choix** : suite à un doublon produit par voyageur-research (4 personas déjà écrits dans `docs/marketing/atelier-acquisition-roi-2026-06.md`, invisible pour lui), audit révélant que `files_hint` (agents-run.js) n'est qu'une liste de CHEMINS texte jamais lue comme contenu, et que `rag-ingest.js` n'ingérait que faits biens + avis + captions. Fix : (A) `scripts/generate-docs-digest.mjs` (mode `_featureDigest.js`) découpe 33 docs de `docs/{marketing,strategie,revenue-manager,crm,service-client,seo,legal}/` par section H2+H3 (435 chunks, `_docsDigest.js` committé) — exclut `docs/superpowers/*` (specs dev) et `docs/_audits/` (transitoire). (B) Vincent a ensuite demandé une fraîcheur quotidienne : nouveau `/api/docs-refresh` (cron Worker `0 13 * * *`) ré-dérive UNIQUEMENT 2 docs FACTUELS depuis une source vivante — trafic SEO 30j (GA4) et signaux marché (D1 `rm_market_signals`) — stockés dans une nouvelle table D1 `docs_snapshots`, ingérés par `rag-ingest.js` en plus des chunks statiques (source=`doc-live`, daté du jour). Le cron enchaîne `docs-refresh` puis `rag-ingest` (sinon le snapshot resterait frais en D1 mais invisible du vecteur jusqu'au prochain lundi).
2. **Alternatives refusées** : auto-rewrite quotidien de TOUS les 33 docs par un agent — rejeté explicitement par Vincent après qu'on a discuté du risque : un LLM qui régénère seul du contenu légal (`legal/contrat-location-meuble-tourisme.md`, `legal/rgpd-check-formulaires.md`...) ou une décision figée (`strategie/plan-ceo-2026-06.md`) sans donnée nouvelle réelle à intégrer n'actualise rien, il hallucine une variation qui s'accumule silencieusement — ligne rouge "éviter l'irréversible". Commit GitHub automatique (fichiers `.md` réellement réécrits) également refusé par Vincent — introduirait un bot avec droit d'écriture non supervisé sur le repo pour un gain marginal, alors qu'une Cloudflare Function n'a de toute façon pas d'accès filesystem en prod pour éditer `docs/` directement.
3. **Conséquences attendues** : tout agent de la fleet peut désormais retrouver via RAG le contenu des docs stratégiques (marketing/legal/SEO/CRM/RM) ET un snapshot trafic/marché daté du jour — mais **33 docs sur 35 restent des snapshots statiques figés** (aucun n'avait été retouché depuis sa création lors de l'audit, jusqu'à 1 mois d'ancienneté) : un agent peut donc citer un budget de campagne ou une reco pricing vieille d'un mois comme si elle était actuelle. Pas encore de tag de fraîcheur inline dans le texte injecté (proposé, pas implémenté) — à considérer si ça cause un vrai souci de citation périmée.
4. **Périmètre** : `scripts/generate-docs-digest.mjs` (nouveau) · `functions/api/_docsDigest.js` (nouveau, généré, 435 chunks) · `functions/api/docs-refresh.js` (nouveau) · `functions/api/rag-ingest.js` (+source docs +source doc-live) · D1 `docs_snapshots` (nouvelle table) · `workers/ical-sync/index.js` (`runDocsRefresh`, cron `0 13 * * *`) · skill `cloture-session` (point 6, régénérer `_docsDigest.js` si les docs changent). `agent_actions.voyageur-004` rejeté (`bloqué`, note pointant vers l'atelier existant).
5. **Statut** : ✅ déployé & vérifié en live à chaque étape (2 bugs trouvés en testant en prod, pas en théorie : découpage H2-seul perdait 3/4 personas par troncature 700 car. → fix H2+H3 ; ids > 64 octets rejetés par Vectorize → hash court + nettoyage de 426 vecteurs orphelins de la 1ère tentative ratée). Ingestion confirmée : 502 puis 504 vecteurs, snapshot du jour retrouvé en 1ère position sur une requête trafic ciblée.

## ADR-AVIS-DELEGATION-VAGUE3-001 · 2026-07-04 · Brouillons de réponse aux avis (dry-run, jamais d'auto-publication)

1. **Choix** : "Projet Délégation · Vague 3" (AGENDA daté 05/07). Avant de construire, vérifié l'infra existante : aucune API d'écriture Google (Business Profile Reviews Reply = accès Google à demander, validation manuelle, pas garanti) ni Airbnb (pas de reply public exposé aux tiers) n'est branchée — seule la LECTURE d'avis existe (`google-reviews.js` Places API, `voyageur_feedback` D1 via scrape Apify). Construit donc un pipeline dry-run sur `voyageur_feedback` (déjà D1-backed, admin UI existante) : `src/utils/reviewClassification.js` (logique pure testée, seuil ≥4★=`auto`/≤3★=`escalade`, toujours escaladé si note absente) + génération LLM (`callLLM` tier smart, style responsable-service-client) stockée en `draft_reply`/`classification`/`draft_status`. `AvisTab.jsx` affiche le brouillon + badge + actions copier/envoyée/ignorer. RIEN n'est publié automatiquement — Vincent copie-colle manuellement.
2. **Alternatives refusées** : construire directement l'auto-publication Google (impossible aujourd'hui, accès API non obtenu) ; classifier ≤3★ comme "auto-publiable un jour" (refusé par design — un avis négatif engage trop pour un pilote automatique, quel que soit l'état futur de l'accès API, donc `escalade` reste permanent pour ces notes même après une hypothétique activation).
3. **Conséquences attendues** : 20 vrais avis traités en prod pour valider le pipeline. Bug trouvé en vérifiant le 1er lot avant de le considérer fini : le prompt n'incluait pas le prénom du voyageur → le LLM adressait parfois la réponse au NOM DU BIEN comme si c'était une personne ("Merci... Géko !") — corrigé (`prenom` passé au prompt, instruction explicite anti-confusion), 20 brouillons régénérés et revérifiés propres. Prochaine étape logique (non faite) : carte "impact" onglet agents + injection du résumé dans le digest hebdo une fois quelques semaines de données accumulées.
4. **Périmètre** : `src/utils/reviewClassification.js` (+ test, nouveau) · `functions/api/voyageur-feedback.js` (+action `draft`, +action `draft-status`, +4 colonnes D1) · `src/tabs/AvisTab.jsx`.
5. **Statut** : ✅ déployé & vérifié en live (20/20 brouillons générés sans échec, qualité relue manuellement avant/après le fix prénom).

## ADR-CAUTION-EXPIRES-001 · 2026-07-04 · Fix `expires_at` caution-checkout (72h→24h, limite Stripe) — endpoint cassé depuis sa création

1. **Choix** : en voulant poser une caution manuelle pour François Cambier (Zandoli, carte jamais enregistrée suite à un incident déjà connu du 2026-06-19 — création de Customer Stripe silencieusement échouée sur son paiement initial), découvert que `caution-checkout.js` posait un `expires_at` à 72h alors que Stripe Checkout Sessions plafonne à 24h — l'endpoint échouait donc sur TOUTE génération de lien depuis sa création, jamais détecté faute d'avoir été testé en conditions réelles. Fixé à 23h (marge sous la limite).
2. **Alternatives refusées** : poser la caution automatiquement sans lien manuel (impossible — pas de carte enregistrée, pas de `customer`/`payment_method` Stripe côté PI initial) ; laisser Vincent décider de ne PAS poser de caution pour cette location précise — décision explicite de Vincent, respectée (aucune caution posée pour Cambier).
3. **Conséquences attendues** : `caution-checkout.js` fonctionne enfin pour tout futur cas de carte non enregistrée (déjà vécu 1 fois avant, se reproduira). Le lien généré expire de lui-même après 24h — rien à annuler côté Vincent s'il choisit de ne pas l'envoyer.
4. **Périmètre** : `functions/api/caution-checkout.js` (1 ligne).
5. **Statut** : ✅ déployé & vérifié en live (lien réel généré avec succès après le fix, session Stripe live confirmée avant que Vincent décide de ne pas s'en servir pour ce cas).

## ADR-MIRROR-DRIFT-001 · 2026-07-04 · Garde-fou anti-drift miroirs GAS/Worker + fix bug latent UTC/local dans REVENUS_AUTO

1. **Choix** : `src/__tests__/mirror-drift.test.js` (nouveau, intégré au gate `npm run test:run`) — équivalence COMPORTEMENTALE (extraction de fonction + `new Function`, pas de comparaison de texte) entre `src/utils/{resaDedup,occupancy}.js` et leurs miroirs GAS/Worker. Cartographie réelle vérifiée (corrige `CLAUDE.md`, qui la surestimait) : seuls `resaDedup.js` et `occupancy.js` ont un vrai miroir dupliqué — `pricing.js`, `coherenceRules.js`, `rmOccupancyAdjust.js` n'en ont AUCUN (les Functions importent le module `src` directement), gardes anti-fork ajoutées pour le détecter si ça change un jour. En écrivant ce test, bug réel trouvé (pas juste théorique) : `nd_` dans `appscript/REVENUS_AUTO_2026/2027.gs` lisait les Date de cellule Sheet en `getUTC*` alors que `toNoonUTC_` du MÊME fichier (et `SCRIPT_SHEETS.normDate_`) les lit en LOCAL — la clé de dédup par contenu pouvait silencieusement être décalée d'1 jour sur une vraie cellule Date. Corrigé + déployé via `clasp deploy @80`.
2. **Alternatives refusées** : comparer les miroirs par diff de texte source (rejeté — cassant à la moindre reformulation cosmétique sans changement de comportement) ; laisser la divergence `normDate` vs `normDate_` (UTC vs local sur une Date à minuit) comme un "bug" à corriger — analysée et actée comme un FIX ASSUMÉ déjà en place côté GAS (la feuille Google stocke les dates à minuit Europe/Paris), documentée en test skip plutôt que "réparée" à nouveau.
3. **Conséquences attendues** : tout futur changement d'un util mirroré sans répercuter le miroir fait maintenant échouer le gate de déploiement au lieu de driver silencieusement en prod. Le fix `nd_` protège rétroactivement `contentKeyRow_`/`contentKeyRow27_` pour toute ligne Sheet lue comme objet Date (pas les strings).
4. **Périmètre** : `src/__tests__/mirror-drift.test.js` (nouveau, 391 lignes) · `CLAUDE.md` (cartographie corrigée) · `appscript/REVENUS_AUTO_2026.gs` + `REVENUS_AUTO_2027.gs` (fix `nd_`). Déployé `clasp push -f` + `clasp deploy -i ...@80`.
5. **Statut** : ✅ déployé & vérifié (389→398 tests verts au fil de la session, 2 skips documentés pour les divergences UTC/local assumées).

## ADR-AGENTS-IMPACT-001 · 2026-07-04 · Endpoint `/api/agents-impact` — mesure d'impact des publications (editorial_calendar × GA4)

1. **Choix** : `GET /api/agents-impact` compare, pour chaque publication `published` du calendrier éditorial, les sessions GA4 des 2 jours avant vs 2 jours après (jour J neutre) — delta absolu + %, résumé (meilleure/pire pub, delta moyen). Logique pure testée dans `src/utils/agentsImpact.js` (15 tests). Le helper GA4 (JWT service account, `runReport`, `parseReport`) a été factorisé de `analytics.js` vers `functions/api/_ga4.js` partagé, réutilisé ensuite par `docs-refresh.js` le même jour — bon signal que l'extraction était la bonne décision.
2. **Alternatives refusées** : dupliquer la logique GA4 dans `agents-impact.js` sans extraire `_ga4.js` (rejeté — l'extraction a immédiatement servi une 2ᵉ fois le jour même pour `docs-refresh.js`, confirmant l'utilité du partage).
3. **Conséquences attendues** : première boucle de feedback quantitative "cette publication a-t-elle fait bouger le trafic ?", réutilisable comme entrée pour une carte "impact" future dans l'onglet agents et pour le digest hebdo.
4. **Périmètre** : `functions/api/agents-impact.js` (nouveau) · `functions/api/_ga4.js` (nouveau, extrait) · `functions/api/analytics.js` (refactor pur, aucun changement de comportement) · `src/utils/agentsImpact.js` (+test).
5. **Statut** : ✅ déployé & vérifié en live (curl direct, deltas réels observés sur des publications de juin : ex. Zandoli "Marché local du samedi" +22 sessions/+28,6%).

## ADR-AGENTS-TRIAGE-001 · 2026-07-04 · Triage hebdo automatique du backlog agents IA (agent_lessons scope + cron + feature-digest)

1. **Choix** : réponse à la demande de Vincent (« peut-on toujours donner un retour aux agents pour qu'ils apprennent ? » puis « fait le » puis « fait évoluer le cron ») — 3 briques construites ensemble : (A) `agent_lessons` gagne une colonne `scope` (`tool` vs `caption`, défaut historique) pour séparer les outils/frameworks hallucinés (Brevo/HubSpot/Slack/Jest/S3, 5 entrées ajoutées) des clichés de captions marketing existants (18 entrées, non touchées) — les deux usages sont incompatibles si mélangés. (B) Nouvel endpoint `/api/agents-triage` + cron Worker lundi 6h UTC (`runAgentsTriage`) : bloque automatiquement les items backlog qui (1) mentionnent un outil banni scope=tool, (2) contredisent un fait bien (`_biens.js` — prix/bookable/capacité), (3) dupliquent un autre item du même lot. (C) 4ᵉ catégorie ajoutée après coup : `_featureDigest.js` (généré par `scripts/generate-feature-digest.mjs`, 88 endpoints + 51 onglets admin, extraits des commentaires d'en-tête) donne un signal FAIBLE « probablement déjà construit » — traité différemment (annotation `🔎`, pas blocage, car le digest est un instantané périodique potentiellement périmé).
2. **Alternatives refusées** : appel live à l'API GitHub pour vérifier le code réel (rejeté — nécessiterait un nouveau secret PAT + une dépendance externe pour un simple cron hebdo, alors qu'un digest statique committé suffit à capter les cas évidents) ; envoyer un brief correctif via `agents-run` à chaque item rejeté (rejeté — coûterait un appel LLM supplémentaire par agent chaque semaine pour une valeur déjà couverte gratuitement par `fetchAgentHistory()`, qui re-fournit automatiquement les notes des items `bloqué` à l'agent concerné au prochain run réel).
3. **Conséquences attendues** : chaque lundi, le backlog agents se nettoie tout seul des 3 catégories fiables + signale (sans bloquer) la 4ᵉ catégorie plus incertaine. **Bug trouvé et corrigé AVANT mise en route réelle** (dry-run révélateur) : les 18 clichés de captions (scope=caption) causaient de vrais faux positifs si mélangés aux bans d'outils (ex. bloquer à tort « faire un Reel sur la vue panoramique » à cause du cliché « vue panoramique... » banni pour les captions) — fixé par la séparation de scope + 2 garde-fous (rejet des verdicts LLM auto-contradictoires, abandon complet si >30% du lot serait bloqué d'un coup).
4. **Périmètre** : `functions/api/agent-lessons.js` (colonne `scope`) · `functions/api/agents-triage.js` (nouveau) · `functions/api/_biens.js` (export `bookable` ajouté) · `functions/api/_featureDigest.js` (nouveau, généré, COMMITTÉ — pas régénéré à chaque build) · `scripts/generate-feature-digest.mjs` (nouveau) · `workers/ical-sync/index.js` (`runAgentsTriage`, cron lundi 6h). Commits `9b6f009`/`8aebed7`/`fcd6bfc`. Skill `cloture-session` mis à jour (point 5, régénérer le digest si `functions/api/*.js` change).
5. **Statut** : **acté & déployé**. Vérifié en live à chaque étape (dry-run avant/après fix : 11 faux positifs → 1 vrai positif restant).

## ADR-MEMORY-SPLIT-001 · 2026-07-04 · Consolidation mémoire — ADR.md/LEARNINGS.md splittés, BLOCKERS/CONTEXT dégraissés

1. **Choix** : `ADR.md` (964 lignes, 2,4× le seuil) split par MOIS calendaire — ne garde que le mois courant, archive le reste dans `.memory/archive/ADR-YYYY-MM.md` (convention qui se reconduit seule chaque mois). `LEARNINGS.md` (838 lignes, 2,1×) split par THÈME (10 fichiers sous `.memory/learnings/`, choisi car aucun cluster chronologique naturel n'existait contrairement à ADR). `BLOCKERS.md` : 20 blocs de sessions « ✅ terminé » (20→26 juin) compactés en 1 ligne chacun, pointant vers `ITERATIONS_LOG.md` (vérifié entrée par entrée que le détail y est réellement présent avant de compacter — une 1ère vérification trop hâtive avait donné une fausse alerte de perte). `CONTEXT.md` : header historique coupé, ne garde que l'état vraiment courant + pointeur.
2. **Alternatives refusées** : split d'ADR.md par thème (rejeté — le fichier est un vrai journal chronologique, un tri thématique casserait l'usage réel de relecture « dernières décisions d'abord ») ; compacter aussi `ITERATIONS_LOG.md` (rejeté après vérification — le fichier a déjà son propre dégradé naturel détail-récent/résumé-ancien, le compacter davantage aurait cassé les pointeurs fraîchement créés dans BLOCKERS/CONTEXT).
3. **Conséquences attendues** : chaque fichier reste sous ~400 lignes. Nouvelle discipline à ancrer : une nouvelle décision ADR va dans `ADR.md` (mois courant, sera archivée automatiquement le mois prochain) ; une nouvelle leçon va directement dans le fichier thématique `learnings/*.md` concerné (jamais dans le `LEARNINGS.md` racine, devenu un pointeur pur).
4. **Périmètre** : `.memory/ADR.md` · `.memory/archive/ADR-2026-06.md` (nouveau) · `.memory/LEARNINGS.md` (devenu pointeur) · `.memory/learnings/*.md` (10 nouveaux + INDEX) · `.memory/BLOCKERS.md` · `.memory/CONTEXT.md` · `.memory/RECALL.md` (+2 lignes) · zone partagée `~/.claude/memory/JOURNAL-locatif.md` (archivé 345→79 lignes) + `CROSS-LEARNINGS.md` (1 boucle fermée). Commit `0f268a5` (locatif) + `ffcb648` (cerveau).
5. **Statut** : **acté & vérifié**. Chaque split vérifié par comptage exact avant/après (131+114=964 lignes ADR restituées à l'identique en bytes de contenu ; 172/172 entrées LEARNINGS catégorisées avant toute écriture, zéro perte).

## ADR-OCCUPANCY-STATS-001 · 2026-07-03 · Endpoint `/api/occupancy-stats` + widget Cockpit (occupation forward 30j par bien)
1. **Choix** : nouvel endpoint `GET /api/occupancy-stats` (reco webmaster) qui LIT le dernier snapshot `rm_kpi_snapshots` (`period_type='30d'`, déjà écrit par le Worker `runOccupancySnapshot`) et renvoie un tableau par bien actif — aucun recalcul, donc aucun mirroir GAS/Worker nécessaire. Surfacé visuellement dans `Cockpit.jsx` (onglet admin par défaut) : chaque carte bien affiche désormais "🔮 30j à venir" sous la barre "vs prorata 2025" existante, en `fetchJSON` + `useEffect` au montage.
2. **Alternatives refusées** : recalculer l'occupation dans le nouvel endpoint (aurait dupliqué la logique déjà dans le Worker, risque de drift) ; l'exposer uniquement en JSON sans UI (Vincent a explicitement demandé "et on le voit où visuellement ?" juste après avoir vu la donnée brute).
3. **Conséquences attendues** : ce widget affiche un 3ᵉ concept d'occupation dans le même onglet, à ne pas confondre avec les 2 déjà présents : la jauge "Occ." de la carte (moyenne historique mensuelle, source Google Sheet) et le KPI portefeuille "Occ. 30j" en tête de Cockpit (rétrospectif, calculé côté client depuis `reservations`, tous biens court-terme confondus). Le nouveau "🔮 30j à venir" est PROSPECTIF et PAR BIEN (nuits déjà réservées pour les 30 prochains jours, snapshot D1). Si un futur chantier touche l'occupation, vérifier lequel des 3 est concerné avant de modifier quoi que ce soit.
4. **Périmètre** : `functions/api/occupancy-stats.js` (nouveau) · `src/tabs/Cockpit.jsx` (`occForward` state + rendu carte).
5. **Statut** : ✅ déployé (commits `9eb80cc`, `ff5c9b2`), endpoint vérifié en direct (curl + Bearer CLAUDE_SECRET, 7 biens retournés avec données cohérentes). **Widget Cockpit non cliqué visuellement** (admin password-gated, Claude n'a pas le mot de passe) — lint/tests/build verts seulement.

## ADR-BUG-INBOX-TRIAGE-001 · 2026-07-03 · Traitement de l'inbox 🐞 Bugs — process de suppression sûre d'une résa confirmée annulée
1. **Choix** : sur demande Vincent ("traites les bugs du dashboard admin"), traité l'inbox `client_errors` via l'API (`?secret=POSTSTAY_SECRET`, pas besoin du mot de passe admin UI) : 8 entrées marquées `ignored` (2 bruit MetaMask, 6 occurrences confirmées transitoires — cf LEARNINGS), 2 alertes `double_booking` (`coherence` critique) laissées visibles jusqu'à validation Vincent. Pour chaque double-réservation, **jamais supprimé sans confirmation explicite de LAQUELLE des deux résas était annulée** (`AskUserQuestion` — 4 options, une par voyageur) avant toute suppression D1.
2. **Alternatives refusées** : deviner quelle résa supprimer à partir du contexte (dates les plus récentes, montant, etc.) — écarté, une suppression de résa est irréversible et peut concerner de l'argent réel (même si ici Booking.com, pas Stripe direct).
3. **Conséquences attendues** : les 2 résas annulées confirmées (Johanna Regnard `booking.com-5366333137` Villa Amaryllis, Anne Ruffin `booking.com-5038096637` Mabouya) supprimées de D1 `direct_bookings` via `wrangler d1 execute --remote`, ciblage exclusif par `payment_intent_id` exact (jamais par nom, jamais de DELETE sans WHERE précis), `COUNT(*)=1` vérifié avant chaque DELETE. Il n'existe **aucun endpoint API dédié** pour supprimer une résa historique (>90j) de `direct_bookings` — `direct-bookings.js` est GET-only et filtré `checkout >= now-90j`. Si ce besoin revient souvent, envisager un endpoint DELETE dédié (admin-only) plutôt que repasser par le CLI à chaque fois.
4. **Périmètre** : D1 `client_errors` (status) · D1 `direct_bookings` (2 lignes supprimées).
5. **Statut** : ✅ fait, vérifié (0 entrée `new`/`backlog` restante dans l'inbox, `COUNT(*)=0` confirmé après chaque DELETE).

## ADR-GEKO-EXCEPTION-001 · 2026-07-03 · Exceptions temporaires Géko (délai 24h + séjour minimum) — auto-expirantes
1. **Choix** : pour débloquer une demande de réservation directe reçue le jour même (checkin 03/07, aujourd'hui), deux exceptions **scopées à Géko uniquement et bornées dans le temps** : (a) un **period override** dans `min_nights_config` (`/api/site-config`, POST direct hors admin UI) — période `2026-07-03→2026-07-05, min:1` insérée EN TÊTE du tableau `periods` de Géko (la recherche s'arrête au premier match, donc l'ordre prime sur la règle "moyenne saison été" déjà en place) ; (b) une exception **code** pour le délai de 24h avant arrivée (`SAME_DAY_BOOKING_BIENS = new Set(["geko"])` + `SAME_DAY_BOOKING_WINDOW = {from:"2026-07-03", to:"2026-07-06"}`, fonction `sameDayBookingAllowed(bienId)` dans `PublicSite.jsx`) — la notice "réservation min 24h à l'avance" est aussi masquée conditionnellement pour ne pas contredire un calendrier qui accepte déjà le jour même.
2. **Alternatives refusées** : modifier le `default` saisonnier de Géko (aurait affecté TOUT l'été, pas seulement ce weekend) ; rendre l'exception permanente (Vincent a explicitement demandé "que pour ce weekend" après une 1ʳᵉ implémentation trop large).
3. **Conséquences attendues** : les deux exceptions **s'auto-désactivent** à la date de fin sans redéploiement ni action de suivi — le period override sort du tableau lors d'un prochain nettoyage de saison (ou reste, inoffensif, car hors de sa plage) ; l'exception same-day (comparaison de date en dur dans le code) devient un no-op après le 06/07 mais **restera dans le code** tant que personne ne la retire — si Vincent redemande une exception similaire plus tard, penser à vérifier si `SAME_DAY_BOOKING_WINDOW`/`SAME_DAY_BOOKING_BIENS` de l'ancienne demande traînent encore avant d'en ajouter une nouvelle.
4. **Périmètre** : D1 `min_nights_config` (clé Géko, via Apps Script PropertiesService) · `src/PublicSite.jsx` (`SAME_DAY_BOOKING_BIENS`, `SAME_DAY_BOOKING_WINDOW`, `sameDayBookingAllowed`, `DateRangePicker` + 2 widgets inline calendrier fiche bien).
5. **Statut** : ✅ déployé et vérifié en LIVE (pas juste en dev) — sélection réelle 3→5 juillet sur `villamaryllis.com/geko` via navigateur piloté, "2 NUITS · 350€", aucun blocage. Zandoli testé en contrôle négatif (toujours bloqué normalement). Commits `6359690` (code) + appel API direct pour le period override (pas de commit, donnée D1/Sheet).

## ADR-ADMIN-KPI-CLARITY-001 · 2026-07-03 · Clarification de 2 libellés KPI ambigus dans le bandeau admin (`TodayBanner`)
1. **Choix** : (a) le libellé "{Mois} — objectif" (gauge circulaire) renommé "{Mois} vs moyenne {année}" + tooltip — ce n'est PAS un objectif fixé manuellement mais la moyenne mensuelle encaissée depuis janvier (`ytdTotal / n`). (b) le libellé "Ce mois" (bloc top/flop 🏆/⚠️) renommé "Top / Flop · YTD {année}" + tooltip — **celui-ci était un vrai bug**, pas juste une formulation ambiguë : le calcul (`sumN(revenus, n)`) additionne janvier→mois courant, pas le mois seul, alors que le panneau juste à côté dit correctement "YTD · Cashflow" pour le même type de calcul.
2. **Alternatives refusées** : aucune — correctifs de libellé purs, aucune logique de calcul modifiée.
3. **Conséquences attendues** : Vincent avait mal interprété les deux chiffres (43k€ Amaryllis / 4,2k€ Mabouya lus comme "ce mois" alors que c'était le cumul janvier-juillet) — plausible vu l'écart de gamme entre biens, mais aurait pu fausser une lecture business future. Aucun autre KPI de `TodayBanner` vérifié suspect (le bloc "YTD · Cashflow" à l'extrême droite était déjà correctement étiqueté).
4. **Périmètre** : `src/App.jsx` (`TodayBanner`, ~L405-458).
5. **Statut** : ✅ déployé (commits `a176844`, `af65ed4`). **Non vérifié visuellement en direct** (admin protégé par mot de passe non disponible pour Claude — lint/tests/build passés, changement texte/tooltip à faible risque). À confirmer par Vincent à l'occasion.

## ADR-REVENUS-REBUILD-AUTOMONTH-001 · 2026-07-03 · `rebuildRevenus2026_` : fromMonth par défaut = mois courant (auto), plus jamais figé

1. **Choix** : le défaut de `fromMonth` (utilisé quand l'appelant ne précise rien) passe d'une constante codée en dur (`4`, ou `6` selon l'action) à `currentMonthDefault_()` = `new Date().getMonth()+1`, calculé à CHAQUE appel. Les 8 points d'entrée (`revenus2026Rebuild{,Dry,Apply,BienDry,BienApply}` en GET+POST) ne codent plus aucun fallback numérique — ils délèguent tous à ce défaut central dans `rebuildRevenus2026_`.
2. **Alternatives refusées** : mettre à jour manuellement la constante chaque mois (c'est exactement ce qui a causé le bug — `4` = avril, jamais retouché depuis, aurait fini par écraser des mois déjà clos si un appel omettait `fromMonth`) ; bloquer purement et simplement tout `fromMonth` < mois courant (rejetée — un rebuild volontaire d'un mois passé pour corriger une erreur reste un cas d'usage légitime, seul le défaut silencieux devait changer).
3. **Conséquences attendues** : un appel de rebuild sans `fromMonth` explicite ne recompute plus jamais que le mois courant → décembre, quel que soit le mois de l'année — protection auto-actualisée sans aucune maintenance humaine future. Un `fromMonth` explicite (y compris dans le passé) reste possible et n'est pas bloqué.
4. **Périmètre** : `appscript/REVENUS_AUTO_2026.gs` (nouvelle fonction `currentMonthDefault_()`, L438-445) · `appscript/SCRIPT_SHEETS.js` (8 lignes de routage GET/POST, retrait des fallbacks `|| 4`/`|| 6`). Déclenché par une question directe de Vincent après avoir vérifié qu'un rebuild manuel (`fromMonth=7` explicite, pour corriger un montant périmé sur la résa Ary Augustin/Zandoli) n'avait pas créé de doublon revenus — juste un chiffre non resynchronisé (898€ au lieu de 710€, corrigé). Déployé `clasp push` + `clasp deploy -i AKfycbw-t5kd…@79`. Commit `4d6135c`.
5. **Statut** : **acté & déployé**. Vérifié en live : `revenus2026RebuildBienDry` sans `fromMonth` retourne désormais `fromMonth:7` (juillet, mois courant réel) au lieu de `4`.

## ADR-GUIDE-PWA-001 · 2026-07-02/03 · PWA installable pour le guide de séjour (/guide-sejour/<bien>)
1. **Choix** : plutôt qu'une app iOS/Android native (proposée par Vincent, déconseillée — coût/délai disproportionnés pour un livret d'accueil), le guide de séjour existant (`GuideSejour.jsx`, déjà en prod, lié depuis les emails J-3/J-1/J-0) devient une **PWA installable** : navigation rapide par ancres (pastilles sticky), bannière d'installation (bouton natif Android/Chrome via `beforeinstallprompt`, instructions manuelles iOS faute d'événement équivalent), **manifest dynamique par bien** injecté via Blob URL (nom/icônes/`start_url` propres à chaque logement, pas un fichier statique par bien à maintenir), icône dédiée chaleureuse (fleur Amaryllis crème/corail générée depuis `amaryllis-mark-cream.svg` + fond navy, PAS l'icône sombre `/icon-192.png` de l'admin), nouveau service worker **scopé** `public/sw-guide.js` (network-first + cache, seulement `/guide-sejour/*` et `/api/guides`, enregistré avec `scope:"/guide-sejour/"` — jamais globalement).
2. **Alternatives refusées** : app native (coût/délai, App Store/Play Store review, maintenance double) ; un unique service worker global (le site a délibérément un kill-switch qui désinscrit tout SW à chaque chargement pour "fiabilité > offline" sur le tunnel de réservation — un SW global aurait dû composer avec ça ou le contourner partout, risqué).
3. **Conséquences attendues** : le kill-switch d'`index.html` a dû être adapté pour **épargner** les registrations/caches `guide-sejour` (sinon il aurait détruit le cache offline à chaque chargement d'une AUTRE page dans un AUTRE onglet, la Cache Storage étant partagée par origine). **Gap identifié après coup, backlog** : le service worker ne cache que le HTML + JSON, pas le bundle JS/CSS (`/assets/*`) — la promesse "fonctionne hors connexion" peut échouer si le cache HTTP normal du navigateur est vidé (tâche #40, `.memory/SPRINT_2026_07.md`). Aucun QR physique ne pointe encore vers cette page (seul chemin actuel = emails) — opportunité loggée (tâche #41).
4. **Périmètre** : `src/GuideSejour.jsx` (QuickNav, InstallPWABanner, injection manifest+apple-touch-icon, registration SW) · `public/sw-guide.js` (nouveau) · `index.html` (kill-switch scopé) · `public/icon-guide-{180,192,512}.png` (nouveaux, générés via `sips` depuis le SVG de marque).
5. **Statut** : ✅ déployé (commit `d246fca`), vérifié en local (dev server + monkey-patch fetch pour contourner un problème connu de `wrangler pages dev` qui 404 sur les assets statiques `/guides/*.json` — limitation locale uniquement, cf LEARNINGS) : nav par ancres, bannière install (simulée via `beforeinstallprompt` synthétique), dismiss+sessionStorage, substitution correcte des icônes apple-touch-icon (les anciennes du site sont désactivées `rel` le temps de la page, pas juste ignorées).

## ADR-HERO-VIDEO-001 · 2026-07-02 · Fond vidéo en boucle sur le hero home (remplace la photo statique)
1. **Choix** : `HeroBrand` (home `PublicSite.jsx`) utilise une vidéo muette en boucle (`public/video/hero-biens-loop.mp4`, 25s/2,9Mo, tour Amaryllis→Zandoli→Géko→Mabouya→Schœlcher) au lieu d'une photo fixe. Layout texte/CTA inchangé (repris tel quel — la 1ʳᵉ version fournie par Vincent avait du texte incrusté dans la vidéo qui obligeait à bricoler le placement ; la version finale sans texte a permis de revenir au layout d'origine).
2. **Alternatives refusées** : garder la vidéo AVEC texte incrusté en repositionnant notre propre texte autour (fait en test — fonctionnait à peu près en desktop, cassait en mobile, jugé pas assez propre) ; tenter de retirer le texte incrusté par ffmpeg (impossible sans inpainting IA — le texte est composité dans les pixels, pas une piste séparée).
3. **Conséquences attendues** : 2 bugs de fond découverts et corrigés en cours de route, à surveiller si le pattern est réutilisé ailleurs (reels, autres pages) : (a) **React ne synchronise pas toujours la PROPRIÉTÉ `.muted`** (juste l'attribut HTML JSX) avant que le navigateur évalue l'autoplay → lecture silencieusement refusée, vidéo figée sur le poster. Fix : `ref` + `useEffect` qui force `.muted = true` puis appelle `.play()` impérativement. (b) **Le navigateur suspend une vidéo de fond en cours de lecture** (économie d'énergie, indépendant de la visibilité de l'onglet) → ajouté un listener `pause` qui relance `.play()` automatiquement.
4. **Périmètre** : `src/PublicSite.jsx` (`HeroBrand`, ~L6861-6970) · `public/video/hero-biens-loop.mp4` · `public/photos/amaryllis/hero-loop-poster.webp`.
5. **Statut** : ✅ déployé et confirmé par Vincent en prod ("oui c'est bon"). Commits `b8455b9`, `0e912b7`, `3cecb28`, `e9afd21`.

## ADR-EDITORIAL-RELIABILITY-001 · 2026-07-01/02 · Fiabilisation de bout en bout de l'auto-publication réseaux
1. **Choix** : 5 correctifs empilés suite au signalement Vincent ("les posts cassent plusieurs fois par semaine") : (a) rotation planner stable par date (`editorial-calendar.js`) au lieu d'une séquence pondérée qui créait des doublons <4j ; (b) gate `hardBlock` corrigé (les noms `missing_photo`/`channels` ne matchaient jamais les vrais filtres `photo`/`forme` → un draft à image invalide pouvait être auto-approuvé par la boucle de réécriture) ; (c) re-fact-check déterministe du caption réécrit avant approbation ; (d) garde Content-Type `image/*` + fallback photo planifiée à la publication (`agent-drafts.js` `executeDraft`) ; (e) **cause racine dominante** : `generateReelDraft` (reels, 3-4/7j du planning) appelait `/api/ai-summary`, qui dépend d'`ANTHROPIC_API_KEY` **absente en prod** → échec systématique et silencieux depuis toujours, rattrapé de façon incohérente par `runEditorialRetry` (regénère en `social_post` via la vraie cascade, d'où l'impression de casse aléatoire). Nouveau `functions/api/llm-generate.js` (proxy `callLLM`) remplace `ai-summary` pour tous les appels serveur-à-serveur du Worker.
2. **Alternatives refusées** : poser `ANTHROPIC_API_KEY` en prod pour réparer `ai-summary.js` tel quel — écarté, ce endpoint n'a de toute façon AUCUN fallback (mono-provider), fragile par construction ; `llm-generate.js` réutilise la cascade résiliente déjà éprouvée par toute la fleet d'agents.
3. **Conséquences attendues** : les reels (3-4/7j) ne devraient plus échouer silencieusement à la génération. Le gate repasse chaque heure pleine (pas seulement à J-2) → une entrée escaladée a une 2ᵉ chance avant son créneau. 2 entrées bloquées guéries manuellement (#151 image hallucinée, #157 doublon score 40/100). À surveiller : si un post rate encore, vérifier D'ABORD `editorial_calendar.status` + `agent_drafts.reviews.gate` avant de re-creuser le pipeline.
4. **Périmètre** : `functions/api/editorial-calendar.js` · `functions/api/editorial-gate.js` · `functions/api/agent-drafts.js` · `functions/api/llm-generate.js` (nouveau) · `workers/ical-sync/index.js` (`generateReelDraft`, gate horaire).
5. **Statut** : ✅ déployé (Pages via CI + `npm run deploy:worker`), 360 tests verts, vérifié en live (endpoint `llm-generate` testé avec le prompt réel de `generateReelDraft`, réponse cascade `mistral-large-2512`). Commits `54a291e`, `65256ee`, `020dbb5`, `eefb41e`.

## ADR-MAINTENANCE-RECURRENCE-001 · 2026-07-01 · Reconduction automatique des entretiens récurrents (clim/piscine)
1. **Choix** : quand une entrée `maintenance` passe à statut "fait" avec un `next_due_at` renseigné, une nouvelle entrée "à_planifier" est auto-créée (même bien/catégorie/titre/prestataire, `scheduled_at = next_due_at`). Garde-fou anti-doublon **par logique de transition fraîche** (statut précédent ≠ "fait" au moment du save/markFait) plutôt qu'un flag DB dédié — pas de migration nécessaire. Cadences suggérées en 1 clic dans le formulaire : clim = +6 mois, piscine = +15 jours (`DEFAULT_INTERVAL`).
2. **Alternatives refusées** : (a) colonne `next_created BOOLEAN` en DB pour marquer "déjà reconduit" — écarté, nécessitait une migration ALTER TABLE sur la table qui venait d'être réparée (cf ADR D1 exec) et la logique de transition fraîche suffit déjà (le bouton "✓ Fait" ne s'affiche que pour les entrées non-fait, et l'édition compare l'ancien statut au nouveau). (b) Cron serveur qui scanne les échéances passées — écarté, plus complexe, alors que le déclenchement au moment même de la clôture est plus simple et immédiat.
3. **Conséquences attendues** : 10 entrées seed créées en prod (clim ×6 biens hors Nogent, piscine ×4 biens avec piscine — Amaryllis/Zandoli/Iguana/Géko). Toute clôture future de ces entrées avec échéance remplie perpétue le cycle sans intervention. Si Vincent oublie de remplir `next_due_at` en clôturant, le cycle s'arrête silencieusement (pas d'alerte) — acceptable pour une v1, amélioration future possible (rappel proactif).
4. **Périmètre** : `src/tabs/MaintenanceTab.jsx` (`createFollowUp`, `DEFAULT_INTERVAL`, bouton suggestion), `functions/api/maintenance.js` (fix D1 exec, prérequis).
5. **Statut** : ✅ déployé (commits `0a72d47` fix D1 + `867af2c` feature) et seedé en prod (10 entrées créées via API, vérifiées par GET).

## ADR-LLM-001 · 2026-07-01 · Migration llama-3.3-70b-versatile → gpt-oss-120b + fallback array + fix plan AI-Ops
1. **Choix** : suite à l'email Groq (décommission `llama-3.3-70b-versatile` le 16/08/2026), migration de `_llm.js` (`MODELS.groq`), `agents-run.js` (`GROQ_MODELS`, découvert code mort), `orchestrator.js` (`ORCH_MODELS`), `ai-ops.js` (`RANK` medium/smart) vers `openai/gpt-oss-120b`. `MODELS.groq.medium/smart` transformés en tableaux (`callLLM()` étendu pour essayer chaque modèle du tableau avant de cascader vers le provider suivant, retry-429 réservé au dernier candidat pour borner la latence). Refresh manuel du plan AI-Ops D1 déclenché (sinon la correction statique reste inerte — le plan prime toujours).
2. **Alternatives refusées** : ne rien faire avant le 16/08 (risque de casse silencieuse) ; laisser le fallback array retry chaque candidat avec backoff complet (testé, latence 50,9s constatée en prod → resserré à 1 tentative par candidat intermédiaire, backoff réservé au dernier).
3. **Conséquences attendues** : plus aucun point de défaillance unique sur un modèle Groq précis dans ce fichier. Découverte structurante : le plan AI-Ops (D1, clé `ai_ops.plan`) **prime toujours** sur `MODELS` statique de `_llm.js` — corriger le statique seul ne suffit pas si le plan existant référence encore l'ancien modèle (vécu : `medium` pointait toujours vers `llama-3.3-70b-versatile` après ma correction, jusqu'au refresh manuel). `GROQ_MODELS` dans `agents-run.js` confirmé code mort (jamais consommé).
4. **Périmètre** : `_llm.js`, `agents-run.js`, `orchestrator.js`, `ai-ops.js`.
5. **Statut** : ✅ déployé via CI (push `70dad15`, `3e68ee8`, `f11a383`). Testé en prod : juriste-compliance (Groq préféré, tier medium) ok 21,6s. Plan D1 refreshé et vérifié propre (`llama-3.3-70b-versatile` absent).

## ADR-LLM-002 · 2026-07-01 · Ajout Kimi K2.6 (Workers AI gratuit) sur tier smart — GLM-5.2/MiniMax M3 écartés
1. **Choix** : `STATIC_CF.smart` (`ai-ops.js`) passe de `@cf/meta/llama-3.3-70b-instruct-fp8-fast` à `@cf/moonshotai/kimi-k2.6` — plus fort en coding/agentique (58,6 SWE-bench Pro), gratuit via Workers AI. `MODELS.cloudflare.smart` (`_llm.js`) synchronisé en filet de secours statique.
2. **Alternatives refusées** : GLM-5.2, MiniMax M3 — vérifiés en direct (Groq/Cerebras `/v1/models` + OpenRouter `pricing`) : absents des 2 premiers, payants sur OpenRouter (pas de variante `:free`). Écartés.
3. **Conséquences attendues** : les agents tier `smart` (community-manager, growth-experiments, etc. — cf `AGENT_TIERS`) peuvent recevoir Kimi K2.6 quand Cloudflare répond en premier dans la cascade `groq→cloudflare→mistral→...`. **Découverte structurante** : `STATIC_CF` est la SEULE source réelle du plan D1 pour `cloudflare` (`buildPlan()` fait `models.cloudflare = {...STATIC_CF}`, aucune discovery live possible sur ce provider — catalogue figé). `MODELS.cloudflare` dans `_llm.js` n'est qu'un filet de secours jamais consulté tant qu'un plan D1 existe (cf LEARNINGS 2026-07-01).
4. **Périmètre** : `functions/api/ai-ops.js` (`STATIC_CF`), `functions/api/_llm.js` (`MODELS.cloudflare`).
5. **Statut** : ✅ déployé (CI push `21d59b0`). Testé : refresh manuel du plan → health-check `cloudflare.smart` `{ok:true, ms:3810, model:'@cf/moonshotai/kimi-k2.6'}`.

## ADR-LOYALTY-001 · 2026-07-01 · Programme de fidélité — paliers + anniversaire + parrainage (3 étapes)

1. **Choix** : programme de fidélité construit en 3 étapes progressives, chacune validée par Vincent avant code (AskUserQuestion sur périmètre + barème) : (1) paliers Bronze (1 séjour) / Argent (2) / Or (3+) affichés dans le CRM, aucune remise auto-appliquée ; (2) email anniversaire de séjour (J+350-380) mentionnant le palier ; (3) parrainage — code -10% filleul RÉUTILISANT le champ promo existant du tunnel (pas de nouveau système), parrain crédité automatiquement 100€ via `stripe-webhook.js` quand le code est utilisé.
2. **Alternatives refusées** : construire un système de parrainage séparé (table dédiée + logique de checkout custom) — rejeté car `promo_codes` + le champ "Code promo" déjà présent au tunnel couvrent déjà 90% du besoin ; il suffisait d'ajouter `referrer_client_id`/`reward_credited` à la table existante. Barème inventé sans données réelles — rejeté : la distribution réelle (`crm_clients`) a été interrogée AVANT de proposer les seuils, révélant que le "client 3 séjours" à LTV 9863€ était en fait Joël Bailleul (bail long Iguana), pas un vrai repeat customer — le barème final exclut Iguana du calcul de palier.
3. **Conséquences attendues** : aucune automatisation d'envoi (les 3 segments CRM — `fidelite` existant, `anniversaire`, `parrainage` — restent déclenchés manuellement via `?dry=1` puis sans, comme le reste du CRM, RM advisory). Le crédit parrain est le SEUL mécanisme 100% automatique de tout le CRM (déclenché par `stripe-webhook.js`, best-effort strict, ne peut jamais faire échouer une confirmation de résa).
4. **Périmètre** : `src/utils/loyaltyTiers.js` (+tests) · `src/utils/referralReward.js` (+tests) · `src/utils/promoCodeGen.js` (+tests, extrait de `promo-codes.js`) · `functions/api/crm-lifecycle.js` (segments anniversaire+parrainage) · `functions/api/stripe-webhook.js` (`creditReferralParrain`) · `functions/api/promo-codes.js` (réutilise le générateur partagé) · `migrations/0006_promo_codes_referral.sql` · `src/tabs/CrmTab.jsx` (badge+encart palier). Commits `3e5ccd0`/`9196712`/`13e9731`.
5. **Statut** : **acté & déployé**. Vérifié en live : dry-run parrainage retourne 2 cibles réelles (nb_sejours=2, Iguana bien exclu), `promo-codes.js` validate toujours fonctionnel après migration.

## ADR-AGENTS-OBSERVABILITY-001 · 2026-07-01 · Onglet Agents — badge VÉRIF, 5 agents manquants, panneau stats LLM

1. **Choix** : 3 améliorations groupées sur `AgentsKanban.jsx` (revue de code d'un diff pré-écrit, pas une conception ex-nihilo) : badge coloré ⚠️ VÉRIF (rouge=haut/orange=moyen) extrait par regex des notes `agents-verify.js`, au lieu d'être noyé dans le texte libre ; `ALL_AGENTS` 23→28 (5 agents fiscal/comptable/notaire/répondeur-social manquants à l'affichage, existaient déjà côté fleet) ; panneau "📊 Stats" branché sur `/api/agents-stats` (endpoint déjà complet, jamais appelé par aucun front avant).
2. **Alternatives refusées** : aucune — le diff proposé réutilisait déjà les bons champs backend, vérifiés champ par champ contre la vraie réponse API avant d'accepter (évite un panneau vide en prod si les noms avaient divergé).
3. **Conséquences attendues** : le panneau stats affiche le coût LLM réel — a révélé au passage que `deepseek-v4-flash`/`deepseek-v4-pro` sont à 100% d'erreurs sur 7j (déjà `disabled` par AI-Ops, donc pas d'action requise mais visible désormais d'un coup d'œil).
4. **Périmètre** : `src/AgentsKanban.jsx` (badge + ALL_AGENTS + StatsPanel + bouton relancer-un-seul-agent ajouté dans un 2ᵉ diff orphelin récupéré en fin de session) · `functions/api/_llm.js` (COST_RATES gpt-oss-120b/qwen3-32b, 2ᵉ diff orphelin). Commits `efe5e91`/`e676a47`.
5. **Statut** : **acté & déployé & vérifié en live** (curl `/api/agents-stats` confirme les champs consommés par `StatsPanel` existent réellement).

## ADR-STRIPE-RECONCILE-001 · 2026-07-01 · Rapprochement Stripe — virements bancaires vs résas

1. **Choix** : nouvel endpoint `functions/api/stripe-reconcile.js` (GET, Bearer admin) liste les payouts Stripe réels (`/v1/payouts` + `/v1/balance_transactions`), calcule brut/frais/net par transaction, et rattache chaque charge à `direct_bookings` via `payment_intent_id`. Nouvel onglet 🏦 Rapprochement Stripe (Finance). Répond à un vrai trou : aucun outil ne montrait ce que Stripe vire RÉELLEMENT après frais.
2. **Alternatives refusées** : aucune — chantier "codé côté sandbox", revu (tests/lint/build + 1 bug isolé `catch(e)` inutilisé corrigé) plutôt que reconçu.
3. **Conséquences attendues** : les résas Airbnb/Booking n'apparaîtront JAMAIS ici (elles ne transitent pas par ce Stripe) — comportement attendu, pas un bug, à ne pas "corriger" plus tard par erreur.
4. **Périmètre** : `functions/api/stripe-reconcile.js` (+tests) · `src/tabs/StripeReconcileTab.jsx` · `src/App.jsx` (nav+render). Commit `6710976`. Aucun secret supplémentaire (réutilise `STRIPE_SECRET_KEY`).
5. **Statut** : **acté & déployé & vérifié en live** (virements réels retournés, rattachement fonctionnel).

## ADR-CALENDAR-SETUP-001 · 2026-07-01 · Sync Google Calendar Ménage — OAuth multi-provider + découverte scopes

1. **Choix** : `_googleOAuth.js` généralisé en multi-provider (un seul Client ID/Secret Google Cloud dessert Gmail ET Calendar, connexions/tokens indépendants en D1). Nouveau `calendar-sync.js` crée/MAJ un event Calendar par ménage (dédup `menage_calendar_events`), invite le prestataire si email connu. Mis en route de bout en bout dans cette session (migration, activation API Calendar, connexion).
2. **Alternatives refusées** : rien à refaire côté Google Cloud pour Calendar (même projet que Gmail) — mais découverte en cours de route que ça ne suffisait PAS : le token émis manquait le scope `calendar.events` car non déclaré dans "Accès aux données" (Google Auth Platform), malgré API activée + utilisateur whitelisté. Fix : déclarer le scope + supprimer la ligne D1 `oauth_tokens` du provider pour forcer un nouveau consentement (voir LEARNINGS pour la procédure complète).
3. **Conséquences attendues** : toute future connexion OAuth Google (3ᵉ provider un jour ?) doit décaler les scopes AVANT le premier essai, pas après un échec silencieux "insufficient authentication scopes".
4. **Périmètre** : `functions/api/_googleOAuth.js`/`gmail-oauth-start.js`/`gmail-oauth-callback.js` (multi-provider) · `functions/api/calendar-sync.js` (+tests) · `migrations/0005_menage_calendar_events.sql` · `src/tabs/MenageTab.jsx`. Commit `8afefef`.
5. **Statut** : **acté & déployé & vérifié en live** (`checked:1, imported:1` équivalent confirmé — sync réel avec vraies entrées geko/zandoli/nogent créées dans `menage_calendar_events`).

## ADR-GMAIL-SETUP-001 · 2026-07-01 · Messagerie Gmail entrante — setup complet OAuth + Google Cloud

1. **Choix** : le chantier « Messagerie Gmail » (codé en amont dans une session sandbox, commit `7ed4ff5`) a été mis en route de bout en bout dans cette session : migration D1 (`0004_emails_log_inbound.sql`), création de l'écran de consentement OAuth (Externe, car compte `vinsmaf@gmail.com` = Gmail perso, pas Workspace → « Interne » indisponible), création de l'ID client OAuth (`Application Web`, redirect `https://villamaryllis.com/api/gmail-oauth-callback`), ajout des secrets `GOOGLE_OAUTH_CLIENT_ID`/`GOOGLE_OAUTH_CLIENT_SECRET` dans Cloudflare Pages, **activation explicite de la Gmail API** (étape manquante — 403 en 1er test), ajout de `contact@villamaryllis.com` comme utilisateur test (obligatoire en mode Externe/Test, sinon Google bloque le consentement), connexion effective par Vincent, vérifié en live (`checked:1, imported:1`).
2. **Alternatives refusées** : type de consentement « Interne » (nécessite Google Workspace, le compte `vinsmaf@gmail.com` est un Gmail perso) — accepté le mode Externe/Test avec le risque connu de refresh_token qui expire au bout de 7 jours tant que l'app n'est pas passée en Production/vérifiée (documenté dans `docs/GMAIL-SETUP.md` §Dépannage).
3. **Conséquences attendues** : le cron Worker (boucle 10 min) importe désormais automatiquement les réponses voyageurs dans `emails_log` (`direction='in'`). **Point de vigilance** : si le refresh token expire après 7j (mode Test), il faudra soit re-consentir manuellement, soit soumettre l'app à validation Google, soit passer le Workspace `villamaryllis.com` en compte Google Workspace pour débloquer le mode Interne.
4. **Périmètre** : Google Cloud project `site-amaryllis` (écran de consentement + client OAuth + API activée) · Cloudflare Pages `dashboard-amaryllis` (2 secrets) · D1 `revenue-manager` (migration 0004 déjà appliquée). Pas de nouveau commit — uniquement config externe (D1/Cloudflare/Google Cloud), le code était déjà dans `7ed4ff5`.
5. **Statut** : **acté & vérifié en live**. Reste à surveiller côté Vincent : expiration refresh_token à J+7 si l'app n'est pas passée en Production.

## ADR-BOOKING-CDP-001 · 2026-07-01 · Scraper Booking.com — bascule sur CDP (Chrome réel de Vincent)

1. **Choix** : `scripts/booking-scraper.mjs` abandonne `chromium.launchPersistentContext()` (profil Playwright dédié, détecté par l'anti-bot Booking.com) au profit de `chromium.connectOverCDP('http://localhost:9222')`, qui pilote le Chrome RÉEL de Vincent (vrais cookies, vraie session déjà connectée). Nouveau script `scripts/booking-chrome-debug.sh` lance Chrome avec `--remote-debugging-port=9222` (ferme l'instance existante si besoin, relance proprement). `npm run booking:login` supprimé du `package.json` (obsolète, remplacé par `npm run booking:chrome`).
2. **Alternatives refusées** : durcir davantage le profil persistant (déjà tenté avec `--disable-blink-features=AutomationControlled` + user-agent custom — insuffisant face à la détection Booking.com) ; scraping headless pur (aggrave la détection).
3. **Conséquences attendues** : le cron local (`crontab -e` sur le Mac de Vincent, hors du contrôle de ce repo) doit désormais lancer `bash scripts/booking-chrome-debug.sh` AVANT `node scripts/booking-scraper.mjs` — sinon le scraper échoue avec « Chrome CDP non disponible ». **Non vérifié cette session** si le crontab existant a été mis à jour en conséquence (friction notée dans BLOCKERS).
4. **Périmètre** : `package.json` (scripts npm) · `scripts/booking-login.mjs` (ajout `channel:'chrome'`, semi-obsolète) · `scripts/booking-scraper.mjs` (refactor CDP) · `scripts/booking-chrome-debug.sh` (nouveau). Commit `56b170e`.
5. **Statut** : **acté & pushé**. Ces scripts sont des automatisations locales (Playwright/bash), hors bundle Cloudflare Pages — le "déploiement" ici = disponibilité dans le repo, pas un changement de prod live.
