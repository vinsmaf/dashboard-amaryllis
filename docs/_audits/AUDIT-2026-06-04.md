# 🕵️ AUDIT — locatif-dashboard · 2026-06-04

> Produit par la skill `auditeur` (lecture seule). Verdict déterministe par dimension, fondé sur des preuves collectées (sorties de commandes), pas sur la mémoire de session.
> Contexte : après la session gouvernance (commits `347f4b3`, `3355740`). Branche `main`, working tree clean.

## Verdict global : 🟡 RISK léger — **déployable, rien de bloquant**
(= pire dimension. Aucun FAIL. Les 🟡 sont des dettes connues / cosmétiques.)

## Tableau de verdict

| Dimension | Statut | Preuve |
|---|---|---|
| Repo | 🟢 PASS | `git status` clean · branche `main` · 5 derniers commits cohérents |
| Tests | 🟢 PASS | `vitest run` → **148 passed (148)**, 13 fichiers |
| Secrets committés | 🟢 PASS | `git grep` clé/secret hors `.md` → 0 fuite réelle (matches = noms de variables, commentaires, prompts) |
| Lint | 🟢 PASS | `npm run lint` → **0 error**, ~17-19 warnings (cosmétiques) |
| Build | ⚪ N/A | non exécuté ce run (gate de deploy le couvre ; 0 code modifié depuis le dernier build vert) |
| INV1 — Source unique des biens | 🟢 PASS | les 4 consommateurs importent `src/data/biens.js` (`functions/[slug].js`, `prerender.mjs`, `_biens.js`, `PublicSite.jsx`) ; aucun **champ prix** codé en dur |
| INV2 — Miroirs GAS/Worker | 🟢 PASS | `resaDedup`→`appscript/{SCRIPT_SHEETS.js,REVENUS_AUTO_2026.gs}` ; `occupancy`→Worker `runOccupancySnapshot/nightsBooked` ; `coherenceRules` importé direct par `coherence-check.js` |
| INV4 — CSP vs tracking | 🟢 PASS | tous les domaines chargés (GA4, googletagmanager, connect.facebook.net, www.facebook.com, js.stripe.com) présents dans `public/_headers` |
| INV5 — Meta SEO ≤ limites | 🟢 PASS | `biens.js` : tous `seoTitle ≤60c` & `seoDesc ≤158c` |
| INV6 — Gouvernance/mémoire | 🟢 PASS | `docs/INDEX.md`, `specs/README.md`, `.memory/*` (6 fichiers) présents & non vides ; `PROJECT_MEMORY.md` = 35 409 o (< 40 000 cible) ; journal archivé |

## Findings escaladés (🟡)
1. **Doc périmée — « 557 erreurs eslint »** : CLAUDE.md et PROJECT_MEMORY affirment ~557 erreurs eslint (justifiant l'exclusion du lint de la CI). Réalité mesurée ce jour : **0 erreur, ~17-19 warnings**. → corriger le wording (le lint pourrait même être réintégré au gate). Escaladé `.memory/BLOCKERS.md`.
2. **Prix littéral dans la prose marketing** : les champs `desc` (texte long) de `functions/[slug].js` contiennent « dès 110€/nuit » en dur. Ce n'est PAS le champ prix (pas de calcul), mais ça **drift** si le prix change. → harmoniser à la main au prochain changement de tarif. Escaladé `.memory/BLOCKERS.md`.
3. **Discipline manuelle des miroirs** (déjà documentée) : les miroirs GAS/Worker existent mais leur synchro reste manuelle → risque latent. Reste en 🟡 dans BLOCKERS.

## Non couvert (par design)
Revue ligne-à-ligne du code → `code-review` · sécurité applicative profonde → `security-review` · décisions métier.
