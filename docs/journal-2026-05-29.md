# Journal de session — 29 mai 2026

Synthèse de tout ce qui a été fait, livré et déployé aujourd'hui sur villamaryllis.com (+ Apps Script + Worker).

---

## 1. Unification des réservations (Google Apps Script)

**Problème** : supprimer une location directe dans l'admin ne mettait pas à jour le Sheet (bug d'id String/Number), et les flux écrivaient dans plusieurs onglets.

**Livré** (`appscript/SCRIPT_SHEETS.js`, déployé via `clasp` sur le projet « Site web Amaryllis », redéployé sur le même deployment id → URL `APPS_SCRIPT_URL` préservée) :
- Tous les flux convergent vers **un seul onglet « Toutes les Réservations »** (13 colonnes).
- `addReservation_` / `deleteReservation_` réécrits, comparaison d'id **en String** (bug corrigé).
- **Action `importBeds24` supprimée** : le mapping Beds24→format unifié se fait désormais côté JS (`Beds24Admin.jsx` + `beds24-webhook.js`) qui appellent directement `importAllReservations` (id `beds24-<bookingId>`, upsert sans doublon).
- Plus d'écriture vers « Réservations Nogent » ni « réservations ».
- Vérifié end-to-end (add → delete → not_found, et chemin prod `/api/sheets-proxy`).

> ⚠️ Geste manuel restant : pour les biens **Beds24 (Nogent)** la suppression directe ne retire pas la résa dans Beds24 (source de vérité) — à annuler dans Beds24.

---

## 2. Réseaux sociaux — bug Facebook corrigé

**Constat** : le post du jour (Amaryllis, carrousel) n'était parti que sur **Instagram**, pas Facebook.

**Cause racine** : le draft avait `channels: ["ig"]`. Le générateur ne forçait `["ig","fb"]` que si le tableau était **vide** → un `["ig"]` émis par le LLM passait.

**Fix** (`functions/api/agents-run.js`) : pour tout `social_post`, les **2 canaux sont forcés** (`["ig","fb"]`). Déployé. Les prochains posts (Mabouya, Zandoli, Nogent) étaient déjà OK.

> Config Meta vérifiée saine (Page 337 abonnés, IG 1029, token valide). Cron horaire publie les entrées `approved` du calendrier éditorial D1.

---

## 3. Backlog agents — 4 lots traités

- **SEO meta (traf-013/019/025)** : titres ≤60c + meta descriptions ≤158c pour les 7 fiches.
  ⚠️ **Découverte clé** : les meta des fiches sont injectées par **`functions/[slug].js`** (runtime, prioritaire), pas par `prerender.mjs`. Corrigé aux 2 endroits.
- **Sécurité** :
  - `arch-012` : **rate limit** D1 60 req/min/IP sur `/api/beds24-bookings`.
  - `arch-018` : **runbook** `docs/runbook-rotation-tokens.md` + **rappel email trimestriel** auto (Worker, jan/avr/juil/oct).
- **Growth** : `growth-003` A/B **hero Amaryllis** (piscine A vs salon B) + conversion = scroll 50% (infra `src/utils/abTest.js`). `growth-002` (prix charm) **déféré** (risque intégrité calcul réservation).
- **Prompt + QA** : `prompt-002` system prompt ChatWidget resserré + `max_tokens` 600→420 (~-30%). `qa-003` smoke test étendu (API social/get-config, sitemap, meta prérendue).
- 8 actions marquées « fait » dans le backlog D1.

---

## 4. Capacité groupe & offre multi-logements

**Recherche par dates (accueil)** : sélecteur voyageurs étendu **1→11**. Au-delà de 8 → affichage des **combinaisons disponibles** (Zandoli 5 / Géko 4 / Mabouya 2) avec prix total live + dispo vérifiée.

**Nudge fiches** : sur Amaryllis/Zandoli/Géko/Mabouya, dès le max atteint → encart vers l'offre groupée.

**Filtre à puces** : ajout « 9+ groupe » → pousse l'offre groupée.

**Capacités confirmées** : Zandoli **5** (4+1 clic-clac), Géko **4** (capacité inchangée), Mabouya **2** → total **11**.

---

## 5. Nomenclature corrigée (logement / cocon / studio)

Seuls **Amaryllis** et **Iguana** sont des « villas ». Corrigé partout (client) :
- **Zandoli** = logement, **Géko** = cocon, **Mabouya** = studio, **Bellevue** = appartement.
- Titres SEO (`functions/[slug].js` + `prerender.mjs`), FAQ, pages guides, offre groupée (« 3 logements »).
- **Agents** : règle de nomenclature ajoutée au prompt partagé (`agents-run.js`) + au skill community-manager (régénéré via `build-skills.mjs`).
- **Fact-checker** (`functions/api/_factcheck.js`) : règle **déterministe** → rejet auto de « Villa Zandoli/Géko/Mabouya/Bellevue ».

---

## 6. Page dédiée offre groupée — `/location-groupe-sainte-luce`

Décision validée après consultation **unanime des agents** (chef-produit, webdesigner, e-business, SEO) : isoler sur une page dédiée + teaser sur l'accueil + fusionner les encarts dupliqués.

**Livré** (HTTP 200, prérendue, sitemap 48 URLs) :
- Hero + H1 + **configurateur fusionné** : dates + voyageurs + cartes sélectionnables (photo + nom + lien fiche + checkbox + prix), **dispo en direct** (anti double-résa), **prix total live**.
- Argumentaire 3 points + FAQ (JSON-LD).
- SEO : title/desc/canonical + injection server-side `functions/[slug].js`.
- **Accueil allégé** : section remplacée par un **teaser-lien** (3 vignettes + CTA « Composer mon séjour groupé »).
- **Maillage anti-cannibalisation** : accueil → 1 lien ; fiches → lien remontant ; page → liens descendants vers les 3 fiches.
- Routing `main.jsx` + meta `prerender.mjs`.

---

## 7. Paiement unique du combo (capture immédiate + alerte hôte)

Choix utilisateur : **capture immédiate + alerte hôte** (+ re-check dispo avant paiement).

**Livré** :
- `GroupPaymentModal` (front) : coordonnées → **re-check dispo** des logements → **un seul PaymentIntent Stripe** pour le total (metadata `type:group`) → Stripe Elements → `/merci`.
- CTA configurateur : **« Réserver et payer → »** (devis en lien secondaire).
- Combos de recherche : CTA `mailto` remplacé par lien vers la page **avec dates/voyageurs pré-remplis** (`?checkin=&checkout=&guests=`) → plus de mail parasite.
- `create-payment-intent.js` : transmet les metadata groupe.
- `stripe-webhook.js` : sur paiement groupe réussi → **alerte hôte « 🚨 bloquer les calendriers »** (Resend) + confirmation voyageur + GA4. (Alerte aussi envoyée côté front en doublon.)

> ⚠️ **Geste manuel par design** : Zandoli/Géko/Mabouya (iCal Airbnb/Booking) ne se bloquent pas automatiquement → à réception de l'alerte, **bloquer les 3 calendriers** côté OTA. (Nogent/Beds24 se bloque seul.)

---

## 8. Publicité Google Ads — kit préparé (non lancé)

Reco timing **confirmée par 4 agents** (traffic, commercial, revenue, e-business) :
- **Juin** : campagne **offre groupée** seule (groupes réservent 4-8 mois à l'avance) + créer l'audience remarketing.
- **Septembre** : push **haute saison déc-avril** (lead-time 3-5 mois).
- Budget 400€ : 60% groupe / 22% brand défensif / 18% remarketing. Ne pas payer pour Amaryllis/Iguana (se remplissent seules).

**Kit prêt à coller** : `docs/google-ads-kit.md` (structure campagnes, mots-clés exact/phrase, 15 négatifs, annonces RSA × 3, extensions, setup conversion `booking_completed`, audience remarketing, checklist + KPIs).

> Bon point technique : `booking_completed` envoie déjà `value`(€) + `currency` (vérifié dans `stripe-webhook.js`) → pas de bloqueur.

**À faire demain (Vincent, sur ordinateur)** : vérifier le crédit 400€ (conditions + expiration), créer l'audience remarketing GA4, activer Google Signals.

---

## Fichiers principaux touchés aujourd'hui
- `appscript/SCRIPT_SHEETS.js`
- `functions/api/agents-run.js`, `_factcheck.js`, `_skills.js` (régénéré), `beds24-bookings.js`, `beds24-webhook.js`, `sheets-proxy.js`, `chat.js`, `create-payment-intent.js`, `stripe-webhook.js`, `[slug].js`
- `src/PublicSite.jsx` (recherche, configurateur, GroupBookingBuilder, GroupPaymentModal, page dédiée, teaser, nudges), `src/main.jsx`, `src/tabs/Beds24Admin.jsx`
- `scripts/prerender.mjs`, `scripts/deploy-pages.sh`
- `workers/ical-sync/index.js` (rappel tokens)
- Docs : `runbook-rotation-tokens.md`, `google-ads-kit.md`, `journal-2026-05-29.md`
- `CLAUDE.md` (section Footguns : double meta, résas unifiées, calendrier éditorial, sécurité, A/B, déploiement)

## Déploiements
- **Cloudflare Pages** `dashboard-amaryllis` (plusieurs fois, smoke test sain).
- **Worker** `amaryllis-ical-sync` (rappel rotation tokens).
- **Apps Script** « Site web Amaryllis » @20 (même URL).

## En attente / prochaines étapes
- Demain : setup Google Ads (audience remarketing + crédit) puis lancement campagne groupe.
- Déféré : `growth-002` (prix charm avec champ `prixAffiche` découplé), mode test Stripe, devis acompte/solde (cpw-100/101).
