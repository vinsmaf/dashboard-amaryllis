# LEGACY — NE PAS MODIFIER

Ces fichiers sont des copies obsolètes des fonctions actives dans `functions/api/`.
Le projet est déployé sur **Cloudflare Pages** — seul `functions/api/` est utilisé en production.
Ces fichiers sont conservés pour référence uniquement.
